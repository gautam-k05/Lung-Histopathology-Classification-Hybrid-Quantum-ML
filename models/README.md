# Models

Trained `.keras` model files are not included in this archive because they are generated during notebook execution and can be large.

The notebook contains the training and saving code for the classical models.
